/**
 * Activity-feed repository layer (migration 20260701120000).
 *
 * Import from `@/repositories/activity`. Intentionally NOT in the root
 * `@/repositories` barrel — same convention as the other domain repo folders.
 */
export { ActivityFeedRepository, activityFeedRepository } from "./activity-feed.repository";
